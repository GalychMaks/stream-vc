_target_: src.data.audio_datamodule.AudioDataModule
data_dir: ${paths.data_dir}
filelist_dir: ${paths.data_dir}
train_filelist: train_filelist.txt
val_filelist: val_filelist.txt
valid_ratio: 0.1
batch_size: 16 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
num_workers: 8
pin_memory: False